#include "raylib.h"

int main(void) {
    // InitWindow(900, 600, "Breakout");

    // while(WindowShouldClose() == false) {
    //     BeginDrawing();
    //     ClearBackground(BLACK);

    //     EndDrawing();
    // }
        
    // CloseWindow();

    return 0;
}